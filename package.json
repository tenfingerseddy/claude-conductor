{
  "name": "conductor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A self-managing harness for Claude: live usage numbers, a rewritable policy file, and memory.",
  "license": "MIT",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
